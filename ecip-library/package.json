{
  "name": "ecip-library-preview",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "check": "node --check src/app.js && node scripts/safety-check.mjs",
    "build": "npm run check && node scripts/build.mjs",
    "dev": "npm run build && python3 -m http.server 4173 --bind 127.0.0.1 -d dist"
  }
}
