# 납품 전 잔여 결함 수정 (B1 · B2 · A3) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 납품처가 804대를 전체 수집하는 5시간 동안 진행 상황을 볼 수 있게 하고(B1), 잘못 만든 예약을 지울 수 있게 하며(B2), 조회 API 인증 전제를 문서와 회귀 테스트로 고정한다(A3).

**Architecture:** BE에 큐 집계 함수와 읽기 전용 엔드포인트를 하나씩 더한다(스키마 변경 없음, 기존 인덱스 `idx_jobs_status_created` 재사용). FE는 순수 계산 라이브러리를 먼저 만들고, 프록시 라우트와 표시 컴포넌트가 그것을 쓴다. 예약 삭제는 기존 `PUT` 경로와 같은 모양으로 `DELETE` 를 추가한다. A3는 코드 변경 없이 런북 문서 + compose 바인딩 회귀 테스트로 끝낸다.

**Tech Stack:** FastAPI · psycopg(dict row factory) · PostgreSQL 16 · Next.js 16 App Router · vitest(node 환경, jsdom 없음) · Python `unittest`

**Spec:** `docs/DELIVERY_READINESS_20260818.md` (항목 B1 / B2 / A3). 점검판: https://claude.ai/code/artifact/95d3f474-dd97-45ff-bf03-41609b30de6f

## Global Constraints

이 절의 내용은 **모든 태스크의 요구사항에 암묵적으로 포함된다.**

- **워커는 살아 있다.** 실존하는 `site_id` 로 작업을 등록하면 실제로 제3자 웹사이트를 크롤한다. 검증에는 반드시 존재하지 않는 `site_id` 를 쓰고, 전후로 `crawl_jobs` 행 수를 기록한다.
- **`PUT /schedules/{site_id}` 를 성공할 요청으로 보내지 않는다.** 통과하면 실제 예약이 생기고 워커가 거기서 작업을 등록하기 시작한다. 승인 경로 검증은 `vitest` 에서 `fetch` 를 mock 해서 "무엇을 전달했을지"만 확인한다(`tests/schedules-route.test.ts` 의 기존 방식 그대로).
- **`git add -A` / `git add .` 금지.** 다른 세션이 같은 브랜치에 파이썬을 커밋한다. 항상 파일을 명시한다.
- **`delivery/fe/tsconfig.json` 과 `delivery/fe/next-env.d.ts` 는 절대 스테이징하지 않는다.** Next가 dev/build에 따라 계속 고쳐 쓴다.
- **운영 DB(`libertree-delivery_pgdata`, 문서 536,056건)에 테스트를 겨누지 않는다.** 파이썬 테스트는 `DROP TABLE` 을 한다. 반드시 아래 임시 컨테이너를 쓴다.
- **운영 스택에 `down -v` 를 실행하지 않는다.**
- 스키마 변경 없음. `delivery/db/schema.py` 를 수정하는 태스크는 이 계획에 하나도 없다.
- 모든 사용자 대면 문자열은 한국어. 기존 화면의 어투를 따른다("…했습니다", "…하세요").

### 테스트 실행 명령 (모든 태스크 공통)

**파이썬 (일회성 postgres를 띄우고 → 테스트 → 정리):**

```bash
cd /mnt/raid/ruci_workspace/frwaler-delivery

docker rm -f plan-testpg >/dev/null 2>&1
docker run -d --rm --name plan-testpg --tmpfs /var/lib/postgresql/data \
  -e POSTGRES_PASSWORD=devpw -e POSTGRES_USER=libertree -e POSTGRES_DB=libertree postgres:16 >/dev/null
until docker exec plan-testpg pg_isready -U libertree -d libertree >/dev/null 2>&1; do sleep 1; done

docker run --rm --network container:plan-testpg \
  -v /mnt/raid/ruci_workspace/frwaler-delivery:/app -w /app \
  -e TEST_PG_DSN='postgresql://libertree:devpw@127.0.0.1:5432/libertree' \
  -e DELIVERY_AUTH_MODE=disabled \
  libertree-delivery-worker:latest python -m unittest tests.test_worker_jobs tests.test_be_app -v

docker rm -f plan-testpg >/dev/null 2>&1
```

`--tmpfs` 와 `--rm` 이 이 격리의 전부다. 이 명령을 바꾸지 말 것.

**프론트엔드:**

```bash
cd /mnt/raid/ruci_workspace/frwaler-delivery/delivery/fe
npx vitest run tests/<파일>.test.ts     # 태스크 중
npm test && npm run typecheck && npm run lint   # 커밋 전
```

기준선: FE 262 tests / 26 files, Python 93 tests. 이 숫자는 태스크마다 늘어난다.

### 수동 확인용 리허설 스택

운영 스택과 격리된 콜드스타트 스택이 이미 떠 있다. 운영 DB를 건드리지 않고 화면을 볼 수 있다.

```
프로젝트   delivery-coldstart      BE 127.0.0.1:8091   FE 127.0.0.1:3091
env 파일   /tmp/claude-1001/-mnt-raid-ruci-workspace-frwaler-delivery-delivery/45a7a0ac-80e4-4b8c-832b-ce53d00c57e2/scratchpad/coldstart/.env.coldstart
래퍼       .../scratchpad/coldstart/dc     (= docker compose -p delivery-coldstart --env-file … -f delivery/docker-compose.yml)
```

내려가 있으면 `"$SP/coldstart/dc" up -d` 로 다시 올린다. 이 스택의 DB는 사이트 804행만 있고 문서·작업은 0건이다.

---

## File Structure

| 파일 | 책임 | 태스크 |
|---|---|---|
| `delivery/worker/jobs.py` (수정) | `summarize_queue(conn)` — 상태별 집계와 평균 소요 시간을 한 곳에서 계산 | 1 |
| `delivery/be/app.py` (수정) | `GET /jobs/summary`, `DELETE /schedules/{site_id}` 라우트 | 2, 5 |
| `delivery/worker/schedules.py` (수정) | `delete(conn, site_id)` — 예약 행 삭제 | 5 |
| `delivery/fe/src/lib/queue-progress.ts` (신규) | BE 응답 → 화면 모델. 순수 함수만. 네트워크·DOM 없음 | 3 |
| `delivery/fe/src/app/api/jobs/summary/route.ts` (신규) | 집계 프록시 (조회이므로 토큰 불필요) | 4 |
| `delivery/fe/src/app/(shell)/job-dashboard.tsx` (수정) | 진행 상황 표시줄 | 4 |
| `delivery/fe/src/app/api/schedules/[siteId]/route.ts` (수정) | `DELETE` 핸들러 추가 (토큰 부착) | 6 |
| `delivery/fe/src/app/(shell)/schedules/schedule-manager.tsx` (수정) | 삭제 버튼 + 확인 | 6 |
| `docs/DELIVERY_DEPLOYMENT_RUNBOOK_20260813.md` (수정) | 노출 전제 명시 + 오늘 고친 2건 반영 | 7 |
| `tests/test_delivery_compose.py` (신규) | compose가 BE를 `127.0.0.1` 에만 묶는지 고정 | 7 |

계산을 `queue-progress.ts` 로 뽑아내는 이유: 이 저장소의 vitest에는 jsdom이 없어 클라이언트 컴포넌트를 렌더할 수 없다. 로직이 `.tsx` 안에 있으면 테스트가 불가능하다. 순수 함수로 빼면 전부 테스트된다.

---

## 이 계획에 없는 것 (의도적)

- **A1 · A2** — 워커가 "예외 없이 끝남"을 "성공"으로 취급하는 문제. 크롤러 반환 계약을 716개 파일에 걸쳐 바꿔야 해서 이 계획의 범위를 넘는다. 스펙에서 납품 후로 분류됨. 크롤러 원문 로그가 보존되므로 개별 진단은 이미 가능하다.
- **조회 API에 토큰 부착** — A3의 대안. FE의 서버 사이드 `fetch` 전부(`getRecentJobs`, `getDatabaseStats`, `getFreshnessStats`, `getVerificationStats`, `/schedules`)가 현재 토큰 없이 호출하므로 함께 고쳐야 하고, 실패 시 화면이 조용히 비는 방식이라 회귀가 눈에 안 띈다. 납품처가 BE를 노출하지 않는다면 불필요하다. Task 7이 그 전제를 문서로 고정한다.
- **스키마 변경 / 마이그레이션** — 필요 없다.

---

### Task 1: BE 큐 집계 함수 `summarize_queue`

FE가 "대기 784건 · 남은 시간 약 4시간 47분"을 말할 수 있게 하는 유일한 데이터 출처.

**Files:**
- Modify: `delivery/worker/jobs.py` (파일 끝, `list_jobs` 아래)
- Test: `tests/test_worker_jobs.py`

**Interfaces:**
- Consumes: 없음 (첫 태스크)
- Produces: `summarize_queue(conn) -> dict` — Task 2가 그대로 JSON으로 돌려준다. 반환 키는 정확히 다음 5개다:

```python
{
  "counts": {"queued": int, "running": int, "cancelling": int,
             "done": int, "failed": int, "cancelled": int},   # 6개 상태 전부, 0이어도 포함
  "active": int,          # queued + running + cancelling
  "finished_24h": int,    # 최근 24시간 안에 끝난 작업 (done+failed+cancelled)
  "avg_seconds": float | None,   # 최근 완료 20건의 평균 소요 시간, 표본 3건 미만이면 None
  "samples": int,         # avg_seconds 를 계산한 표본 수
}
```

**설계 판단 두 가지 — 반드시 이대로 구현할 것:**

1. **평균은 `done` 만으로 낸다.** `cancelled` 를 넣으면 대기 중 취소되어 `started_at IS NULL` 이거나 시작 직후 죽은 작업이 평균을 0 쪽으로 끌어내려 "10분 남음"이라고 거짓말하게 된다. 이 저장소는 이미 뭉개진 타임스탬프 때문에 한 번 잘못된 결론을 냈다.
2. **`total`(전체 이력 건수)은 반환하지 않는다.** 작업 이력은 영구 보존이라, 전체 대비 진행률은 지난 몇 달치에 희석되어 의미가 없다. 진행률은 `finished_24h` 와 `active` 로만 계산한다(Task 3).

- [ ] **Step 1: 실패하는 테스트를 작성한다**

`tests/test_worker_jobs.py` 의 `WorkerJobsTest` 클래스 안, `test_enqueue_and_claim` 바로 아래에 추가한다:

```python
    def test_summarize_queue_counts_every_status_including_zero(self):
        from delivery.worker import jobs
        jobs.enqueue_job(self.conn, "s1", requested_by="op")
        jobs.enqueue_job(self.conn, "s2", requested_by="op")
        summary = jobs.summarize_queue(self.conn)
        self.assertEqual(summary["counts"]["queued"], 2)
        # 0인 상태도 키가 있어야 화면이 "실행 중 0건"을 말할 수 있다.
        for status in ("queued", "running", "cancelling", "done", "failed", "cancelled"):
            self.assertIn(status, summary["counts"])
        self.assertEqual(summary["counts"]["done"], 0)
        self.assertEqual(summary["active"], 2)

    def test_summarize_queue_needs_three_samples_before_estimating(self):
        from delivery.worker import jobs
        for index in range(2):
            job_id = jobs.enqueue_job(self.conn, f"done{index}", requested_by="op")
            self.conn.execute(
                """UPDATE crawl_jobs SET status='done',
                     started_at=now()-interval '30 seconds', finished_at=now() WHERE id=%s""",
                (job_id,))
        self.conn.commit()
        summary = jobs.summarize_queue(self.conn)
        self.assertEqual(summary["samples"], 2)
        # 표본 2건으로 "약 5시간"을 말하면 그건 추정이 아니라 추측이다.
        self.assertIsNone(summary["avg_seconds"])

    def test_summarize_queue_estimates_from_completed_runs_only(self):
        from delivery.worker import jobs
        for index in range(3):
            job_id = jobs.enqueue_job(self.conn, f"done{index}", requested_by="op")
            self.conn.execute(
                """UPDATE crawl_jobs SET status='done',
                     started_at=now()-interval '20 seconds', finished_at=now() WHERE id=%s""",
                (job_id,))
        # 대기 중 취소된 작업: started_at 이 없다. 평균에 들어오면 추정이 0쪽으로 무너진다.
        cancelled = jobs.enqueue_job(self.conn, "cancelled-in-queue", requested_by="op")
        self.conn.execute(
            "UPDATE crawl_jobs SET status='cancelled', finished_at=now() WHERE id=%s", (cancelled,))
        self.conn.commit()
        summary = jobs.summarize_queue(self.conn)
        self.assertEqual(summary["samples"], 3)
        self.assertAlmostEqual(summary["avg_seconds"], 20.0, delta=2.0)
        self.assertEqual(summary["finished_24h"], 4)
        self.assertEqual(summary["active"], 0)
```

- [ ] **Step 2: 테스트를 돌려 실패를 확인한다**

Run: Global Constraints의 파이썬 명령에서 `tests.test_worker_jobs` 만 지정
Expected: FAIL — `AttributeError: module 'delivery.worker.jobs' has no attribute 'summarize_queue'` 3건

- [ ] **Step 3: 최소 구현을 작성한다**

`delivery/worker/jobs.py` 의 `list_jobs` 함수 바로 아래에 추가한다:

```python
JOB_STATUSES = ("queued", "running", "cancelling", "done", "failed", "cancelled")

# 추정을 시작하기 전에 요구하는 완료 표본 수. 1~2건으로 낸 평균은 추정이 아니라
# 추측이고, 화면은 그 차이를 표현할 수 없으므로 여기서 아예 None을 돌려준다.
_MIN_ESTIMATE_SAMPLES = 3
_ESTIMATE_WINDOW = 20


def summarize_queue(conn) -> dict:
    """Queue depth by status plus an honest per-site duration estimate.

    Deliberately returns no all-time total: job history is retained forever
    (verification aggregates are computed from it), so a percentage against it
    would be diluted by months of past runs. Progress is expressed against
    finished_24h + active instead.
    """
    counts = {status: 0 for status in JOB_STATUSES}
    for row in conn.execute("SELECT status, count(*) AS n FROM crawl_jobs GROUP BY status").fetchall():
        counts[row["status"]] = int(row["n"])

    finished_24h = int(conn.execute(
        """SELECT count(*) AS n FROM crawl_jobs
            WHERE finished_at IS NOT NULL AND finished_at > now() - interval '24 hours'"""
    ).fetchone()["n"])

    # Only 'done' runs. A job cancelled out of the queue never started, so its
    # duration is null or near zero; letting those in drags the estimate toward
    # "almost finished" precisely when a bulk sweep has just been stopped.
    estimate = conn.execute(
        """SELECT avg(EXTRACT(EPOCH FROM (finished_at - started_at)))::float8 AS avg_seconds,
                  count(*) AS samples
             FROM (SELECT started_at, finished_at FROM crawl_jobs
                    WHERE status='done' AND started_at IS NOT NULL AND finished_at IS NOT NULL
                    ORDER BY finished_at DESC LIMIT %s) recent""",
        (_ESTIMATE_WINDOW,),
    ).fetchone()
    samples = int(estimate["samples"])
    avg_seconds = float(estimate["avg_seconds"]) if samples >= _MIN_ESTIMATE_SAMPLES else None

    return {
        "counts": counts,
        "active": counts["queued"] + counts["running"] + counts["cancelling"],
        "finished_24h": finished_24h,
        "avg_seconds": avg_seconds,
        "samples": samples,
    }
```

- [ ] **Step 4: 테스트를 돌려 통과를 확인한다**

Run: 같은 명령
Expected: PASS. `tests.test_worker_jobs` 가 기존 건수 + 3

- [ ] **Step 5: 커밋한다**

```bash
cd /mnt/raid/ruci_workspace/frwaler-delivery
git add delivery/worker/jobs.py tests/test_worker_jobs.py
git commit -m "feat(delivery): summarize the crawl queue with an honest duration estimate

804-site sweeps run for hours and nothing anywhere reports how far along they
are. summarize_queue returns depth per status plus a mean duration taken from
the last 20 completed runs.

Two deliberate omissions. The estimate uses 'done' only: a job cancelled out
of the queue never started, and letting those in pulls the mean toward zero
exactly when a sweep has just been stopped. And no all-time total is returned
-- job history is retained forever, so a percentage against it would be
diluted by months of past runs.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: `GET /jobs/summary` 라우트

**Files:**
- Modify: `delivery/be/app.py` (`@app.get("/jobs")` 핸들러 바로 위)
- Test: `tests/test_be_app.py`

**Interfaces:**
- Consumes: Task 1의 `jobs.summarize_queue(conn) -> dict`
- Produces: `GET /jobs/summary` → 200, 본문은 `summarize_queue` 의 반환값 그대로. Task 4의 프록시가 이 모양에 의존한다.

**설계 판단:** `/stats`·`/freshness` 와 달리 **TTL 캐시(`aggregates`)를 쓰지 않는다.** 그 캐시는 30초이고, 이 값은 조작 직후 즉시 반영되어야 한다 — "전체 중지"를 눌렀는데 대기 건수가 30초 동안 그대로면 운영자는 중지가 안 먹혔다고 판단하고 한 번 더 누른다. 쿼리는 `idx_jobs_status_created` 를 쓰는 GROUP BY 하나라 캐시할 만큼 비싸지도 않다.

- [ ] **Step 1: 실패하는 테스트를 작성한다**

`tests/test_be_app.py` 의 `test_missing_operator_token_configuration_fails_closed` 바로 위에 추가한다:

```python
    def test_job_summary_reports_queue_depth_without_a_token(self):
        response = self.client.get("/jobs/summary")
        self.assertEqual(response.status_code, 200)
        body = response.json()
        self.assertIn("counts", body)
        self.assertIn("queued", body["counts"])
        self.assertIn("active", body)
        self.assertIn("finished_24h", body)
        self.assertIn("samples", body)
        # 표본이 없으면 추정치를 지어내지 않는다.
        self.assertIsNone(body["avg_seconds"])

    # /jobs/{job_id} 가 먼저 매칭되어 "summary"를 작업 번호로 읽어버리면 안 된다.
    def test_job_summary_is_not_shadowed_by_the_job_detail_route(self):
        self.assertEqual(self.client.get("/jobs/summary").status_code, 200)
```

- [ ] **Step 2: 테스트를 돌려 실패를 확인한다**

Run: `tests.test_be_app`
Expected: FAIL — `/jobs/summary` 가 `GET /jobs/{job_id}` 에 잡혀 422(정수 아님) 또는 404

- [ ] **Step 3: 최소 구현을 작성한다**

`delivery/be/app.py` 에서 **`@app.get("/jobs/{job_id}")` 보다 위**에 넣는다. FastAPI는 선언 순서대로 매칭하므로 순서가 곧 정확성이다:

```python
    # Declared before /jobs/{job_id}: FastAPI matches in declaration order, and
    # the literal path must win over the parameterised one.
    #
    # Not behind the aggregate TTL cache, unlike /stats and /freshness. Its
    # 30-second window would leave the queue count unchanged for half a minute
    # after a bulk stop, and an operator who cannot see their cancellation land
    # presses it again. The query is one GROUP BY over idx_jobs_status_created.
    @app.get("/jobs/summary")
    def get_job_summary():
        conn = _conn()
        try:
            return jobs.summarize_queue(conn)
        finally:
            conn.close()
```

- [ ] **Step 4: 테스트를 돌려 통과를 확인한다**

Run: `tests.test_be_app`
Expected: PASS (기존 건수 + 2)

- [ ] **Step 5: 리허설 스택에서 실물로 확인한다**

```bash
SP=/tmp/claude-1001/-mnt-raid-ruci-workspace-frwaler-delivery-delivery/45a7a0ac-80e4-4b8c-832b-ce53d00c57e2/scratchpad
"$SP/coldstart/dc" build be && "$SP/coldstart/dc" up -d be
until [ "$(docker inspect delivery-coldstart-be-1 --format '{{.State.Health.Status}}')" = healthy ]; do sleep 3; done
curl -s http://127.0.0.1:8091/jobs/summary
```

Expected: `{"counts":{"queued":0,...},"active":0,"finished_24h":0,"avg_seconds":null,"samples":0}`
(이 스택은 작업 0건이므로 전부 0이 정상이다.)

- [ ] **Step 6: 커밋한다**

```bash
cd /mnt/raid/ruci_workspace/frwaler-delivery
git add delivery/be/app.py tests/test_be_app.py
git commit -m "feat(delivery): serve the crawl queue summary

Declared above /jobs/{job_id} so the literal path wins the ordered match, and
deliberately outside the aggregate TTL cache: a 30-second window would leave
the queue count frozen after a bulk stop, and an operator who cannot see their
cancellation land presses stop again.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: FE 진행 상황 계산 라이브러리

이 저장소의 vitest에는 jsdom이 없다. 계산이 `.tsx` 안에 있으면 테스트할 수 없으므로 순수 함수로 분리한다.

**Files:**
- Create: `delivery/fe/src/lib/queue-progress.ts`
- Test: `delivery/fe/tests/queue-progress.test.ts`

**Interfaces:**
- Consumes: Task 2의 응답 모양
- Produces:

```ts
export type QueueSummaryPayload = { counts?, active?, finished_24h?, avg_seconds?, samples? }
export type QueueProgress = Readonly<{
  queued: number; running: number; active: number; finished: number;
  percent: number | null;      // 0-100, 진행 중일 때만. 없으면 null
  etaText: string | null;      // "약 4시간 47분 남음" 또는 null
  headline: string;            // "대기 784건 · 실행 중 1건"
}>;
export const parseQueueSummary: (payload: unknown) => QueueProgress | null
export const formatEta: (seconds: number | null, activeJobs: number) => string | null
```

- [ ] **Step 1: 실패하는 테스트를 작성한다**

`delivery/fe/tests/queue-progress.test.ts` 를 새로 만든다:

```ts
import { describe, expect, it } from "vitest";
import { formatEta, parseQueueSummary } from "../src/lib/queue-progress";

const payload = (over: Record<string, unknown> = {}) => ({
  counts: { queued: 784, running: 1, cancelling: 0, done: 19, failed: 0, cancelled: 0 },
  active: 785, finished_24h: 19, avg_seconds: 22, samples: 19, ...over,
});

describe("parseQueueSummary", () => {
  it("names the two numbers an operator is actually waiting on", () => {
    const progress = parseQueueSummary(payload())!;
    expect(progress.queued).toBe(784);
    expect(progress.running).toBe(1);
    expect(progress.headline).toContain("784");
    expect(progress.headline).toContain("실행 중 1건");
  });

  // 작업 이력은 영구 보존이라 전체 대비 진행률은 지난 몇 달치에 희석된다.
  // 진행률은 이번 작업분(최근 24시간 완료 + 남은 것)에 대해서만 낸다.
  it("measures progress against the current sweep, not all of history", () => {
    const progress = parseQueueSummary(payload())!;
    expect(progress.percent).toBe(Math.round((19 / (19 + 785)) * 100));
  });

  it("has no progress bar when nothing is queued or running", () => {
    const progress = parseQueueSummary(payload({
      counts: { queued: 0, running: 0, cancelling: 0, done: 19, failed: 0, cancelled: 0 },
      active: 0,
    }))!;
    expect(progress.percent).toBeNull();
    expect(progress.etaText).toBeNull();
    expect(progress.headline).toBe("대기 중인 작업이 없습니다.");
  });

  it("counts a cancelling job as still occupying the worker", () => {
    const progress = parseQueueSummary(payload({
      counts: { queued: 0, running: 0, cancelling: 2, done: 0, failed: 0, cancelled: 0 },
      active: 2,
    }))!;
    expect(progress.active).toBe(2);
  });

  it("refuses a malformed payload rather than rendering NaN", () => {
    expect(parseQueueSummary(null)).toBeNull();
    expect(parseQueueSummary({})).toBeNull();
    expect(parseQueueSummary({ counts: "nope", active: 1 })).toBeNull();
    expect(parseQueueSummary(payload({ active: "many" }))).toBeNull();
  });
});

describe("formatEta", () => {
  // 워커는 한 번에 한 사이트씩 처리하는 단일 직렬 루프다. 따라서 남은 시간은
  // 남은 건수 x 평균 소요 시간이다.
  it("multiplies the mean run by the jobs still waiting", () => {
    expect(formatEta(22, 785)).toBe("약 4시간 47분 남음");
  });

  it("drops to minutes and then to a floor", () => {
    expect(formatEta(22, 100)).toBe("약 36분 남음");
    expect(formatEta(2, 10)).toBe("1분 미만 남음");
  });

  // 표본이 모자라면 BE가 null을 준다. 그때 숫자를 지어내지 않는다.
  it("says nothing when there is no measurement to base it on", () => {
    expect(formatEta(null, 785)).toBeNull();
    expect(formatEta(22, 0)).toBeNull();
  });
});
```

- [ ] **Step 2: 테스트를 돌려 실패를 확인한다**

Run: `cd delivery/fe && npx vitest run tests/queue-progress.test.ts`
Expected: FAIL — `Failed to resolve import "../src/lib/queue-progress"`

- [ ] **Step 3: 최소 구현을 작성한다**

`delivery/fe/src/lib/queue-progress.ts` 를 새로 만든다:

```ts
// Turns the backend's queue summary into the two sentences an operator needs
// while a 804-site sweep runs for hours: how much is left, and how long that
// will take. Kept free of React and fetch so it can be tested -- this repo's
// vitest has no jsdom, so anything living inside a client component is
// untestable by construction.

export type QueueProgress = Readonly<{
  queued: number;
  running: number;
  active: number;
  finished: number;
  percent: number | null;
  etaText: string | null;
  headline: string;
}>;

const SECONDS_PER_MINUTE = 60;
const SECONDS_PER_HOUR = 3600;

const isCount = (value: unknown): value is number =>
  typeof value === "number" && Number.isFinite(value) && value >= 0;

// The worker is a single serial loop -- one site at a time -- so the wait is
// simply the queue depth times how long one site has been taking.
export const formatEta = (seconds: number | null, activeJobs: number): string | null => {
  if (seconds === null || !isCount(seconds) || !isCount(activeJobs) || activeJobs === 0) return null;
  const total = Math.round(seconds * activeJobs);
  if (total < SECONDS_PER_MINUTE) return "1분 미만 남음";
  if (total < SECONDS_PER_HOUR) return `약 ${Math.round(total / SECONDS_PER_MINUTE)}분 남음`;
  const hours = Math.floor(total / SECONDS_PER_HOUR);
  const minutes = Math.round((total % SECONDS_PER_HOUR) / SECONDS_PER_MINUTE);
  return minutes === 0 ? `약 ${hours}시간 남음` : `약 ${hours}시간 ${minutes}분 남음`;
};

export const parseQueueSummary = (payload: unknown): QueueProgress | null => {
  if (typeof payload !== "object" || payload === null) return null;
  const body = payload as Record<string, unknown>;
  const counts = body.counts;
  if (typeof counts !== "object" || counts === null) return null;
  const byStatus = counts as Record<string, unknown>;

  const queued = byStatus.queued, running = byStatus.running;
  const active = body.active, finished = body.finished_24h;
  if (!isCount(queued) || !isCount(running) || !isCount(active) || !isCount(finished)) return null;

  const avgSeconds = body.avg_seconds;
  const mean = isCount(avgSeconds) ? avgSeconds : null;

  if (active === 0) {
    return { queued, running, active, finished, percent: null, etaText: null,
      headline: "대기 중인 작업이 없습니다." };
  }

  // Against the current sweep, never against all-time history: job records are
  // retained forever, so an all-time denominator would report a fresh 804-site
  // run as already 90% done.
  const denominator = finished + active;
  return {
    queued, running, active, finished,
    percent: denominator === 0 ? null : Math.round((finished / denominator) * 100),
    etaText: formatEta(mean, active),
    headline: `대기 ${queued.toLocaleString("ko-KR")}건 · 실행 중 ${running.toLocaleString("ko-KR")}건`,
  };
};
```

- [ ] **Step 4: 테스트를 돌려 통과를 확인한다**

Run: `npx vitest run tests/queue-progress.test.ts`
Expected: PASS (10 tests)

- [ ] **Step 5: 커밋한다**

```bash
cd /mnt/raid/ruci_workspace/frwaler-delivery
git add delivery/fe/src/lib/queue-progress.ts delivery/fe/tests/queue-progress.test.ts
git commit -m "feat(fe): compute queue progress and a remaining-time estimate

Pure functions, no React and no fetch: this repo's vitest has no jsdom, so
logic that lives inside a client component cannot be tested at all.

Progress is measured against the current sweep (recent completions plus what
is still queued), never against all-time history -- job records are retained
forever, and an all-time denominator would report a fresh 804-site run as
already nearly done.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: 진행 상황 프록시와 화면 표시

**Files:**
- Create: `delivery/fe/src/app/api/jobs/summary/route.ts`
- Create: `delivery/fe/tests/jobs-summary-route.test.ts`
- Modify: `delivery/fe/src/app/(shell)/job-dashboard.tsx`
- Modify: `delivery/fe/src/app/globals.css` (표시줄 스타일)

**Interfaces:**
- Consumes: Task 2의 `GET /jobs/summary`, Task 3의 `parseQueueSummary` / `QueueProgress`
- Produces: `GET /api/jobs/summary` → BE 응답을 그대로 전달. 실패 시 `{ error: string }` 과 503.

**설계 판단:** 이 프록시는 **토큰을 붙이지 않는다.** `/jobs/summary` 는 조회이고 BE의 다른 조회와 마찬가지로 `require_operator` 가 없다. 기존 `GET /api/jobs` 와 정확히 같은 모양이다.

- [ ] **Step 1: 실패하는 테스트를 작성한다**

`delivery/fe/tests/jobs-summary-route.test.ts` 를 새로 만든다:

```ts
import { afterEach, describe, expect, it, vi } from "vitest";

// "server-only" is a Next.js build-time guard with no runtime resolution
// outside the bundler; stub it so the route module imports under plain vitest.
vi.mock("server-only", () => ({}));

const { GET } = await import("../src/app/api/jobs/summary/route");

const summary = { counts: { queued: 3, running: 1, cancelling: 0, done: 0, failed: 0, cancelled: 0 },
  active: 4, finished_24h: 0, avg_seconds: null, samples: 0 };

describe("GET /api/jobs/summary", () => {
  afterEach(() => { vi.unstubAllGlobals(); });

  it("passes the backend summary through untouched", async () => {
    vi.stubGlobal("fetch", vi.fn(async () => new Response(JSON.stringify(summary), { status: 200 })));
    const response = await GET();
    expect(response.status).toBe(200);
    expect(await response.json()).toEqual(summary);
  });

  it("forwards a backend failure status rather than inventing a summary", async () => {
    vi.stubGlobal("fetch", vi.fn(async () => new Response("{}", { status: 500 })));
    expect((await GET()).status).toBe(500);
  });

  // 진행 표시줄이 사라지는 것과 "0건 남음"이라고 말하는 것은 완전히 다르다.
  it("reports a connection failure instead of a zeroed queue", async () => {
    vi.stubGlobal("fetch", vi.fn(async () => { throw new Error("ECONNREFUSED"); }));
    const response = await GET();
    expect(response.status).toBe(503);
    expect((await response.json() as { error: string }).error).toContain("진행 상황");
  });
});
```

- [ ] **Step 2: 테스트를 돌려 실패를 확인한다**

Run: `npx vitest run tests/jobs-summary-route.test.ts`
Expected: FAIL — 모듈을 찾을 수 없음

- [ ] **Step 3: 프록시 라우트를 작성한다**

`delivery/fe/src/app/api/jobs/summary/route.ts` 를 새로 만든다:

```ts
import { NextResponse } from "next/server";

const backendUrl = (): string => (process.env.BE_URL ?? "http://127.0.0.1:8080").replace(/\/$/, "");

// No operator token: the backend's /jobs/summary is a read, like every other
// GET it serves. Mirrors GET /api/jobs exactly.
export async function GET(): Promise<NextResponse> {
  try {
    const response = await fetch(`${backendUrl()}/jobs/summary`, {
      cache: "no-store",
      signal: AbortSignal.timeout(10_000),
    });
    const result = await response.json().catch(() => ({})) as Record<string, unknown>;
    return NextResponse.json(result, { status: response.status });
  } catch (error) {
    const detail = error instanceof Error ? error.message : String(error);
    // Never fall back to zeros: a hidden progress bar and a bar reading
    // "0 remaining" say opposite things to the person watching it.
    return NextResponse.json({ error: "진행 상황을 불러올 수 없습니다.", detail }, { status: 503 });
  }
}
```

- [ ] **Step 4: 테스트를 돌려 통과를 확인한다**

Run: `npx vitest run tests/jobs-summary-route.test.ts`
Expected: PASS (3 tests)

- [ ] **Step 5: JobDashboard에 표시줄을 붙인다**

`delivery/fe/src/app/(shell)/job-dashboard.tsx` 를 수정한다. 세 군데다.

(a) import에 추가:

```tsx
import { parseQueueSummary, type QueueProgress } from "@/lib/queue-progress";
```

(b) `const [jobs, setJobs] = useState<Job[]>([]);` 바로 아래에 상태를 추가:

```tsx
  const [progress, setProgress] = useState<QueueProgress | null>(null);
```

그리고 기존 `load` 콜백 안, `setJobs(...)` 다음 줄에 집계 요청을 추가한다. 목록과 별개로 실패해야 한다 — 집계가 죽어도 작업 목록은 계속 보여야 한다:

```tsx
      // Fetched alongside the list but failing independently: losing the
      // progress bar must not also blank the job table.
      try {
        const summary = await fetch("/api/jobs/summary", { cache: "no-store" });
        setProgress(summary.ok ? parseQueueSummary(await summary.json()) : null);
      } catch { setProgress(null); }
```

(c) 컴포넌트가 반환하는 JSX의 맨 위, 작업 표 바로 앞에 표시줄을 넣는다:

```tsx
      {progress && (
        <div className="queue-progress" role="status" aria-live="polite">
          <div className="queue-progress-line">
            <strong>{progress.headline}</strong>
            {progress.etaText && <span className="queue-progress-eta">{progress.etaText}</span>}
          </div>
          {progress.percent !== null && (
            <div className="queue-progress-track">
              <div className="queue-progress-fill" style={{ width: `${progress.percent}%` }} />
            </div>
          )}
          {progress.active > 0 && progress.etaText === null && (
            <p className="queue-progress-note">
              완료된 작업이 아직 적어 남은 시간을 추정하지 않습니다.
            </p>
          )}
        </div>
      )}
```

(d) `delivery/fe/src/app/globals.css` 끝에 추가한다:

```css
.queue-progress { display: flex; flex-direction: column; gap: .5rem; margin-bottom: 1rem; }
.queue-progress-line { display: flex; align-items: baseline; justify-content: space-between; gap: 1rem; }
.queue-progress-eta { font-variant-numeric: tabular-nums; color: var(--muted); font-size: .9rem; }
.queue-progress-track { height: 6px; border-radius: 3px; background: var(--sand-wash); overflow: hidden; }
.queue-progress-fill { height: 100%; background: var(--moss); transition: width .4s ease; }
.queue-progress-note { margin: 0; font-size: .85rem; color: var(--muted); }
```

**토큰 이름은 확인된 것만 썼다.** `globals.css` 의 `:root` 가 정의하는 토큰은 다음이
전부다 — 여기 없는 이름을 쓰면 `tests/css-tokens.test.ts` 가 실패한다(과거에 정의되지
않은 토큰을 써서 화면이 깨진 적이 있어 그 테스트가 생겼다: `664b0e8`).

```
--canvas  --surface  --ink  --muted  --rule
--moss  --moss-deep  --moss-wash  --sand-wash
--bad  --bad-wash  --sans  --serif  --mono
```

- [ ] **Step 6: 전체 테스트·타입·린트를 돌린다**

```bash
cd /mnt/raid/ruci_workspace/frwaler-delivery/delivery/fe
npm test && npm run typecheck && npm run lint
```

Expected: 전부 통과. 테스트 수는 262 + 13 = 275

- [ ] **Step 7: 리허설 스택에서 눈으로 확인한다**

```bash
SP=/tmp/claude-1001/-mnt-raid-ruci-workspace-frwaler-delivery-delivery/45a7a0ac-80e4-4b8c-832b-ce53d00c57e2/scratchpad
"$SP/coldstart/dc" build fe && "$SP/coldstart/dc" up -d fe
until [ "$(docker inspect delivery-coldstart-fe-1 --format '{{.State.Health.Status}}')" = healthy ]; do sleep 3; done
curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:3091/
curl -s http://127.0.0.1:3091/api/jobs/summary
```

Expected: `200`, 그리고 `{"counts":{...},"active":0,...}`.
브라우저에서 http://127.0.0.1:3091/ 를 열면 작업이 0건이므로 표시줄은 "대기 중인 작업이 없습니다."가 되어야 한다.

**FE 빌드가 실패하면**: `tests/route-rendering.test.ts` 를 먼저 돌려 볼 것. 서버 데이터를 읽는 새 페이지에 `force-dynamic` 이 빠지면 이 빌드가 깨진다(그 결함으로 한 번 막혔다).

- [ ] **Step 8: 커밋한다**

```bash
cd /mnt/raid/ruci_workspace/frwaler-delivery
git add delivery/fe/src/app/api/jobs/summary/route.ts \
        delivery/fe/tests/jobs-summary-route.test.ts \
        'delivery/fe/src/app/(shell)/job-dashboard.tsx' \
        delivery/fe/src/app/globals.css
git commit -m "feat(fe): show queue depth and remaining time while a sweep runs

A full 804-site collection takes about five hours and the console showed 20
rows, no total and no estimate. The likeliest ending was an operator deciding
it had hung, cancelling, and starting over -- hours lost and every third-party
site hit again.

The summary is fetched alongside the job list but fails independently: losing
the progress bar must not blank the table too, and a failed fetch hides the
bar rather than rendering zeros, because a hidden bar and a bar reading
\"0 remaining\" say opposite things.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: BE 예약 삭제 (B2)

**Files:**
- Modify: `delivery/worker/schedules.py`
- Modify: `delivery/be/app.py` (`@app.put("/schedules/{site_id}")` 바로 아래)
- Test: `tests/test_worker_jobs.py`, `tests/test_be_app.py`

**Interfaces:**
- Consumes: 없음
- Produces:
  - `schedules.delete(conn, site_id: str) -> bool` — 지웠으면 True, 그런 예약이 없었으면 False
  - `DELETE /schedules/{site_id}` → 200 `{"deleted": true, "site_id": "..."}` / 404 / 401

**안전성 근거(확인 완료):** `crawl_schedules` 를 참조하는 외래키는 없다. 예약이 만든 작업은 `crawl_jobs.requested_by='scheduler'` 라는 문자열만 갖고 있어 예약 행과 참조 관계가 없다. 따라서 행 삭제는 **이미 만들어진 작업에 아무 영향이 없다** — 예약은 사라지고, 진행 중이던 수집은 그대로 끝난다. 이 성질을 Step 1의 테스트가 고정한다.

- [ ] **Step 1: 실패하는 테스트를 작성한다**

`tests/test_worker_jobs.py` 에 추가한다:

```python
    def test_delete_schedule_removes_it_and_reports_whether_it_existed(self):
        from delivery.worker import schedules
        schedules.upsert(self.conn, site_id="doomed", interval_hours=24, created_by="test")
        self.assertTrue(schedules.delete(self.conn, "doomed"))
        self.assertEqual(schedules.list_schedules(self.conn), [])
        # 두 번째 호출은 오류가 아니라 "없었다"이다.
        self.assertFalse(schedules.delete(self.conn, "doomed"))

    # 예약을 지우는 것과 이미 등록된 작업을 취소하는 것은 다른 행동이다.
    # 삭제가 조용히 진행 중인 수집을 죽이면 안 된다.
    def test_delete_schedule_leaves_the_jobs_it_already_created(self):
        from delivery.worker import schedules
        schedules.upsert(self.conn, site_id="doomed", interval_hours=24, created_by="test")
        self.conn.execute("UPDATE crawl_schedules SET next_run_at=now() WHERE site_id='doomed'")
        self.conn.commit()
        self.assertEqual(schedules.enqueue_due(self.conn), 1)
        schedules.delete(self.conn, "doomed")
        remaining = self.conn.execute(
            "SELECT count(*) AS n FROM crawl_jobs WHERE site_id='doomed'").fetchone()["n"]
        self.assertEqual(remaining, 1)
```

`tests/test_be_app.py` 에 추가한다:

```python
    def test_delete_schedule_requires_an_operator_token(self):
        with mock.patch.dict(os.environ, {"DELIVERY_AUTH_MODE": "token", "DELIVERY_API_TOKEN": "x" * 32}):
            response = self.client.delete("/schedules/whatever")
        self.assertEqual(response.status_code, 401)

    def test_delete_unknown_schedule_reports_404(self):
        self.assertEqual(self.client.delete("/schedules/never-scheduled").status_code, 404)
```

- [ ] **Step 2: 테스트를 돌려 실패를 확인한다**

Run: `tests.test_worker_jobs tests.test_be_app`
Expected: FAIL — `module 'delivery.worker.schedules' has no attribute 'delete'`, 그리고 DELETE에 405

- [ ] **Step 3: 최소 구현을 작성한다**

`delivery/worker/schedules.py` 의 `list_schedules` 바로 아래:

```python
def delete(conn, site_id: str) -> bool:
    """Remove a schedule. Returns False when there was nothing to remove.

    Nothing references crawl_schedules, and jobs it created carry only the
    string requested_by='scheduler', so deleting the row cannot disturb work
    already queued or running -- the schedule stops repeating, the current
    collection finishes.
    """
    row = conn.execute(
        "DELETE FROM crawl_schedules WHERE site_id=%s RETURNING id", (site_id,)
    ).fetchone()
    conn.commit()
    return row is not None
```

`delivery/be/app.py` 의 `put_schedule` 바로 아래:

```python
    @app.delete("/schedules/{site_id}")
    def delete_schedule(site_id: str, operator: str = Depends(require_operator)):
        conn = _conn()
        try:
            if not schedules.delete(conn, site_id):
                raise HTTPException(status_code=404, detail="schedule not found")
            return {"deleted": True, "site_id": site_id}
        finally:
            conn.close()
```

- [ ] **Step 4: 테스트를 돌려 통과를 확인한다**

Run: `tests.test_worker_jobs tests.test_be_app`
Expected: PASS (worker +2, be +2)

- [ ] **Step 5: 커밋한다**

```bash
cd /mnt/raid/ruci_workspace/frwaler-delivery
git add delivery/worker/schedules.py delivery/be/app.py \
        tests/test_worker_jobs.py tests/test_be_app.py
git commit -m "feat(delivery): delete a crawl schedule

The backend offered only GET and PUT, so a schedule created by mistake could
be disabled but never removed, and stayed on the list for the life of the
deployment.

Nothing references crawl_schedules and its jobs carry only the string
requested_by='scheduler', so the row deletes cleanly -- the schedule stops
repeating and work already queued finishes untouched. A test holds that
apart, because deleting a schedule and cancelling its running collection are
different actions and must stay different.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 6: FE 예약 삭제 프록시와 버튼

**Files:**
- Modify: `delivery/fe/src/app/api/schedules/[siteId]/route.ts`
- Modify: `delivery/fe/src/app/(shell)/schedules/schedule-manager.tsx`
- Test: `delivery/fe/tests/schedules-route.test.ts`

**Interfaces:**
- Consumes: Task 5의 `DELETE /schedules/{site_id}`
- Produces: `DELETE /api/schedules/{siteId}` → 200 / 404 / 503

- [ ] **Step 1: 실패하는 테스트를 작성한다**

`delivery/fe/tests/schedules-route.test.ts` 의 맨 아래에 추가한다. 파일 위쪽의 import를 먼저 고친다:

```ts
const { PUT, DELETE } = await import("../src/app/api/schedules/[siteId]/route");
```

그리고 파일 끝에:

```ts
describe("DELETE /api/schedules/[siteId]", () => {
  afterEach(() => { vi.unstubAllGlobals(); });

  const del = (siteId: string) =>
    DELETE(new NextRequest(`http://localhost/api/schedules/${siteId}`, { method: "DELETE" }),
      { params: Promise.resolve({ siteId }) });

  it("forwards the delete with the operator token attached", async () => {
    const fetchMock = vi.fn(async () => new Response(JSON.stringify({ deleted: true }), { status: 200 }));
    vi.stubGlobal("fetch", fetchMock);
    expect((await del("some-site")).status).toBe(200);
    const [url, init] = fetchMock.mock.calls[0] as [string, RequestInit];
    expect(url).toContain("/schedules/some-site");
    expect(init.method).toBe("DELETE");
  });

  it("passes a missing schedule through as 404", async () => {
    vi.stubGlobal("fetch", vi.fn(async () => new Response("{}", { status: 404 })));
    expect((await del("gone")).status).toBe(404);
  });

  // 경로를 벗어나는 site id를 백엔드까지 보내지 않는다.
  it("rejects a site id that would escape the path", async () => {
    const fetchMock = vi.fn();
    vi.stubGlobal("fetch", fetchMock);
    expect((await del("a/b")).status).toBe(422);
    expect(fetchMock).not.toHaveBeenCalled();
  });

  it("reports an unreachable backend instead of claiming success", async () => {
    vi.stubGlobal("fetch", vi.fn(async () => { throw new Error("ECONNREFUSED"); }));
    expect((await del("some-site")).status).toBe(503);
  });
});
```

- [ ] **Step 2: 테스트를 돌려 실패를 확인한다**

Run: `npx vitest run tests/schedules-route.test.ts`
Expected: FAIL — `DELETE is not a function`

- [ ] **Step 3: 프록시에 DELETE를 추가한다**

`delivery/fe/src/app/api/schedules/[siteId]/route.ts` 의 `PUT` 함수 아래에 추가한다:

```ts
export async function DELETE(request: NextRequest, context: { params: Promise<{ siteId: string }> }) {
  const { siteId } = await context.params;
  if (!siteId || siteId.includes("/")) return NextResponse.json({ detail: "invalid site" }, { status: 422 });

  try {
    const response = await fetch(`${backend()}/schedules/${encodeURIComponent(siteId)}`, {
      method: "DELETE",
      headers: operatorHeaders(),
      cache: "no-store",
      signal: AbortSignal.timeout(8000),
    });
    if (response.status === 404) {
      return NextResponse.json({ detail: "이미 삭제된 예약입니다." }, { status: 404 });
    }
    if (!response.ok) {
      return NextResponse.json({ detail: "예약을 삭제하지 못했습니다." }, { status: response.status });
    }
    return NextResponse.json({ deleted: true, site_id: siteId }, { status: 200 });
  } catch {
    return NextResponse.json({ detail: SCHEDULE_BACKEND_UNREACHABLE_MESSAGE }, { status: 503 });
  }
}
```

`request` 는 쓰지 않지만 Next의 라우트 시그니처를 유지하기 위해 남긴다. eslint가 미사용 인자를 지적하면 `_request` 로 바꾼다.

- [ ] **Step 4: 테스트를 돌려 통과를 확인한다**

Run: `npx vitest run tests/schedules-route.test.ts`
Expected: PASS (기존 + 4)

- [ ] **Step 5: 삭제 버튼을 붙인다**

`delivery/fe/src/app/(shell)/schedules/schedule-manager.tsx` 의 `save` 함수 바로 아래에 추가한다:

```tsx
  // 두 단계로 나눈 이유: 예약 목록은 한 화면에 여러 줄이고, 되돌리기가 없다.
  const [confirming, setConfirming] = useState<string | null>(null);

  const remove = async (siteId: string) => {
    setMessage(null);
    let response: Response;
    try {
      response = await fetch(`/api/schedules/${encodeURIComponent(siteId)}`, { method: "DELETE" });
    } catch {
      setMessage({ text: SCHEDULE_SAVE_NETWORK_ERROR_MESSAGE, error: true });
      return;
    }
    if (!response.ok) {
      setMessage({ text: `${siteId} 예약을 삭제하지 못했습니다.`, error: true });
      return;
    }
    setRows((old) => old.filter((item) => item.site_id !== siteId));
    setConfirming(null);
    setMessage({ text: `${siteId} 예약을 삭제했습니다. 이미 등록된 작업은 그대로 진행됩니다.`, error: false });
  };
```

그리고 예약 행을 그리는 `<tr>` 의 마지막 `<td>` 안(저장 버튼 옆)에 넣는다:

```tsx
                {confirming === row.site_id ? (
                  <>
                    <button type="button" onClick={() => void remove(row.site_id)}>삭제 확인</button>
                    <button type="button" onClick={() => setConfirming(null)}>취소</button>
                  </>
                ) : (
                  <button type="button" onClick={() => setConfirming(row.site_id)}>삭제</button>
                )}
```

**구현 전에 반드시 파일을 읽고 실제 `<td>` 구조에 맞출 것.** 이 컴포넌트는 한 줄로 압축된 JSX를 쓰는 곳이 있어 위 조각을 그대로 붙여넣으면 형태가 안 맞을 수 있다.

- [ ] **Step 6: 전체 테스트·타입·린트를 돌린다**

```bash
cd /mnt/raid/ruci_workspace/frwaler-delivery/delivery/fe
npm test && npm run typecheck && npm run lint
```

Expected: 전부 통과

- [ ] **Step 7: 리허설 스택에서 실물로 확인한다**

**여기서만 실제 예약을 만든다.** 리허설 스택은 문서 0건에 격리되어 있고, 만들 예약도 즉시 지운다. 운영 스택(8081)에는 절대 보내지 않는다.

```bash
SP=/tmp/claude-1001/-mnt-raid-ruci-workspace-frwaler-delivery-delivery/45a7a0ac-80e4-4b8c-832b-ce53d00c57e2/scratchpad
TOKEN=$(grep '^DELIVERY_API_TOKEN=' "$SP/coldstart/.env.coldstart" | cut -d= -f2-)
SITE=$(curl -s 'http://127.0.0.1:8091/freshness' | python3 -c 'import json,sys;print(json.load(sys.stdin)["sites"][0]["site_id"])')

# 워커가 집어가기 전에 지울 수 있도록 주기를 길게 준다
curl -s -X PUT "http://127.0.0.1:8091/schedules/$SITE" -H "x-delivery-token: $TOKEN" \
  -H 'content-type: application/json' \
  -d '{"interval_hours":8760,"mode":"incremental","limit_n":1,"enabled":false}' >/dev/null
curl -s http://127.0.0.1:8091/schedules | grep -c "$SITE"          # 1 이어야 함
curl -s -X DELETE "http://127.0.0.1:8091/schedules/$SITE" -H "x-delivery-token: $TOKEN"
curl -s http://127.0.0.1:8091/schedules                             # {"schedules":[]}
docker exec delivery-coldstart-postgres-1 psql -U libertree -d libertree -tAc 'select count(*) from crawl_jobs'
```

Expected: 삭제 후 목록이 비고, **`crawl_jobs` 는 0건 그대로**(`enabled:false` 라 작업이 만들어지지 않는다).

- [ ] **Step 8: 커밋한다**

```bash
cd /mnt/raid/ruci_workspace/frwaler-delivery
git add 'delivery/fe/src/app/api/schedules/[siteId]/route.ts' \
        'delivery/fe/src/app/(shell)/schedules/schedule-manager.tsx' \
        delivery/fe/tests/schedules-route.test.ts
git commit -m "feat(fe): remove a schedule from the console

Two-step confirm: the list holds many rows at once and there is no undo. The
success message says explicitly that jobs already queued keep running, since
deleting a schedule and cancelling its collection are different actions.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 7: 조회 API 노출 전제 확정 (A3) + 런북 갱신

코드 변경 없음. 전제를 문서로 고정하고, 그 전제가 조용히 깨지지 않도록 회귀 테스트를 건다.

**Files:**
- Create: `tests/test_delivery_compose.py`
- Modify: `docs/DELIVERY_DEPLOYMENT_RUNBOOK_20260813.md`

**Interfaces:**
- Consumes: 없음
- Produces: 없음 (문서 + 테스트)

**전제:** BE는 프록시나 방화벽 뒤에 둔다. compose가 BE를 `127.0.0.1` 에만 바인딩하는 것이 이 전제의 유일한 기술적 근거이므로, 그 한 줄이 바뀌면 테스트가 깨져야 한다.

- [ ] **Step 1: 실패하는 테스트를 작성한다**

`tests/test_delivery_compose.py` 를 새로 만든다:

```python
# -*- coding: utf-8 -*-
"""Holds the deployment premise that the delivery runbook documents.

Seventeen GET endpoints carry no operator token -- the entire document corpus
is readable by anyone who can reach the port. What keeps that safe today is
one character in docker-compose.yml: the published port is bound to
127.0.0.1, not 0.0.0.0. If that binding ever loosens, the runbook's premise
is silently false, so it fails here instead.
"""
import os
import re
import unittest

COMPOSE = os.path.join(os.path.dirname(__file__), "..", "delivery", "docker-compose.yml")


class DeliveryComposeBindingTest(unittest.TestCase):
    def setUp(self):
        with open(COMPOSE, encoding="utf-8") as handle:
            self.compose = handle.read()

    def test_published_ports_bind_to_loopback_only(self):
        published = re.findall(r'^\s*-\s*"([^"]+:\d+|\$\{[^}]+\}:\d+|[^"]*)"\s*$',
                               self.compose, flags=re.MULTILINE)
        mappings = [entry for entry in published if re.search(r":\d+$", entry) and "->" not in entry]
        self.assertTrue(mappings, "no published port mappings found -- has the compose file moved?")
        for mapping in mappings:
            self.assertTrue(
                mapping.startswith("127.0.0.1:"),
                f"published port {mapping!r} is not bound to loopback; the runbook premise "
                "that GET endpoints are unreachable from outside no longer holds",
            )

    def test_the_backend_port_is_still_published_through_a_variable(self):
        # Documents the exact line the premise rests on, so a rename is visible.
        self.assertIn('"127.0.0.1:${BE_PORT:-8080}:3001"', self.compose)
        self.assertIn('"127.0.0.1:${FE_PORT:-3000}:3002"', self.compose)
```

- [ ] **Step 2: 테스트를 돌려 통과하는지 확인한다**

이 테스트는 **처음부터 통과해야 한다** — 현재 상태를 고정하는 것이 목적이기 때문이다. TDD의 예외로, 대신 아래로 진짜 회귀를 잡는지 증명한다:

```bash
cd /mnt/raid/ruci_workspace/frwaler-delivery
docker run --rm -v /mnt/raid/ruci_workspace/frwaler-delivery:/app -w /app \
  libertree-delivery-worker:latest python -m unittest tests.test_delivery_compose -v
```

Expected: 2 tests PASS (`TEST_PG_DSN` 불필요 — DB를 쓰지 않는다)

- [ ] **Step 3: 테스트가 실제로 회귀를 잡는지 증명한다**

```bash
cd /mnt/raid/ruci_workspace/frwaler-delivery
cp delivery/docker-compose.yml /tmp/compose.bak
sed -i 's|"127.0.0.1:${BE_PORT:-8080}:3001"|"0.0.0.0:${BE_PORT:-8080}:3001"|' delivery/docker-compose.yml
docker run --rm -v /mnt/raid/ruci_workspace/frwaler-delivery:/app -w /app \
  libertree-delivery-worker:latest python -m unittest tests.test_delivery_compose 2>&1 | tail -5
cp /tmp/compose.bak delivery/docker-compose.yml
git diff --stat delivery/docker-compose.yml    # 아무것도 안 나와야 한다
```

Expected: 되돌리기 전에 FAIL 2건, 되돌린 뒤 `git diff` 는 비어 있음

- [ ] **Step 4: 런북에 전제를 명시한다**

`docs/DELIVERY_DEPLOYMENT_RUNBOOK_20260813.md` 끝에 절을 추가한다:

```markdown
## 노출 범위 — 반드시 지켜야 할 전제 (2026-08-18 확정)

**BE를 인터넷이나 사내망에 직접 노출하지 마세요.** 프록시나 방화벽 뒤에 두거나,
같은 호스트의 FE만 접근하게 두어야 합니다.

조회 API 17개에는 운영자 토큰이 걸려 있지 않습니다. 쓰기 9개 중 8개만 토큰을
요구합니다(`POST /translation/preview` 는 동사만 POST일 뿐 `SELECT count(*)` 만
하는 조회입니다). 즉 **포트에 닿을 수 있는 사람은 문서 전체를 읽을 수 있습니다.**

지금 이것을 막고 있는 것은 `delivery/docker-compose.yml` 의 다음 두 줄뿐입니다.

    ports:
      - "127.0.0.1:${BE_PORT:-8080}:3001"
      - "127.0.0.1:${FE_PORT:-3000}:3002"

`127.0.0.1:` 을 지우거나 `0.0.0.0:` 으로 바꾸면 이 전제는 즉시 깨집니다.
`tests/test_delivery_compose.py` 가 그 변경을 실패로 잡습니다.

노출이 꼭 필요하다면 조회 API에도 토큰을 걸어야 하며, 그때는 FE의 서버 사이드
호출(`getRecentJobs`, `getDatabaseStats`, `getFreshnessStats`,
`getVerificationStats`, `/schedules`)이 전부 토큰 없이 나가고 있으므로 함께
고쳐야 합니다. 이들은 실패 시 화면이 조용히 비는 방식이라 빠뜨리면 눈에 띄지
않습니다.

## 2026-08-18 수정 반영

이 런북이 작성된 뒤 납품을 막는 결함 2건이 수정되었습니다.

- **FE 이미지 빌드 실패** (`4897df6`): `/schedules` 에 `force-dynamic` 이 없어
  `next build` 가 실패했습니다. 이 런북의 빌드 절차는 그대로 유효하지만,
  `4897df6` 이전 커밋으로는 FE 이미지를 만들 수 없습니다.
- **예시 API 토큰** (`e502494`): `.env.example` 은 이제 `DELIVERY_API_TOKEN` 을
  빈 값으로 배포합니다. **`openssl rand -hex 32` 로 직접 생성해서 채우세요.**
  비워 두거나 옛 예시 문자열을 그대로 쓰면 BE가 시작을 거부합니다.
```

- [ ] **Step 5: 커밋한다**

```bash
cd /mnt/raid/ruci_workspace/frwaler-delivery
git add tests/test_delivery_compose.py docs/DELIVERY_DEPLOYMENT_RUNBOOK_20260813.md
git commit -m "docs(delivery): pin the deployment exposure premise and test it

Seventeen GET endpoints carry no operator token, so anyone who can reach the
port can read the whole corpus. What makes that acceptable is a deployment
premise -- the backend sits behind a proxy -- resting entirely on one
127.0.0.1 prefix in docker-compose.yml, written down nowhere.

State the premise in the runbook and add a test that fails if the binding
loosens, so it cannot become silently false. Also record the two blocking
fixes made after this runbook was written.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 8: 통합 확인과 납품 문서 갱신

전부 합쳐진 상태에서 한 번 더 돌리고, 스펙 문서의 상태를 갱신한다.

**Files:**
- Modify: `docs/DELIVERY_READINESS_20260818.md`

**Interfaces:**
- Consumes: Task 1~7 전부

- [ ] **Step 1: 파이썬 전체 스위트를 돌린다**

```bash
cd /mnt/raid/ruci_workspace/frwaler-delivery
docker rm -f plan-testpg >/dev/null 2>&1
docker run -d --rm --name plan-testpg --tmpfs /var/lib/postgresql/data \
  -e POSTGRES_PASSWORD=devpw -e POSTGRES_USER=libertree -e POSTGRES_DB=libertree postgres:16 >/dev/null
until docker exec plan-testpg pg_isready -U libertree -d libertree >/dev/null 2>&1; do sleep 1; done
docker run --rm --network container:plan-testpg \
  -v /mnt/raid/ruci_workspace/frwaler-delivery:/app -w /app \
  -e TEST_PG_DSN='postgresql://libertree:devpw@127.0.0.1:5432/libertree' \
  -e DELIVERY_AUTH_MODE=disabled \
  libertree-delivery-worker:latest python -m unittest \
    tests.test_be_app tests.test_delivery_cache tests.test_delivery_catalogue \
    tests.test_delivery_compose tests.test_delivery_database tests.test_delivery_freshness \
    tests.test_delivery_migration tests.test_delivery_seed tests.test_delivery_stats \
    tests.test_delivery_verification tests.test_worker_cycle tests.test_worker_jobs \
    tests.test_worker_run
docker rm -f plan-testpg >/dev/null 2>&1
```

Expected: OK. 93 + 11 = 104 tests

- [ ] **Step 2: FE 전체를 돌린다**

```bash
cd /mnt/raid/ruci_workspace/frwaler-delivery/delivery/fe
npm test && npm run typecheck && npm run lint
```

Expected: 262 + 17 = 279 tests, typecheck·lint 통과

- [ ] **Step 3: 리허설 스택을 처음부터 다시 세운다**

이 계획은 BE와 FE를 모두 바꿨다. 콜드스타트가 여전히 되는지 확인한다 — 그것이 이 항목들이 나온 자리다.

```bash
SP=/tmp/claude-1001/-mnt-raid-ruci-workspace-frwaler-delivery-delivery/45a7a0ac-80e4-4b8c-832b-ce53d00c57e2/scratchpad
"$SP/coldstart/dc" down -v          # 리허설 스택만. 운영 스택 아님.
"$SP/coldstart/dc" build
"$SP/coldstart/dc" --profile bootstrap run --rm migrate && "$SP/coldstart/dc" up -d
until [ "$("$SP/coldstart/dc" ps --format '{{.Health}}' | grep -c healthy)" -ge 3 ]; do sleep 3; done
for r in / /crawlers /collected /documents /schedules /translations; do
  printf "%-14s %s\n" "$r" "$(curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:3091$r)"
done
curl -s http://127.0.0.1:3091/api/jobs/summary
```

Expected: `migrate` 가 804행 등록, 6개 화면 전부 200, 집계 JSON 응답

- [ ] **Step 4: 스펙 문서를 갱신한다**

`docs/DELIVERY_READINESS_20260818.md` 에서 B1·B2·A3 항목의 제목에 완료 표시를 하고, 아래 형식으로 결과를 적는다:

```markdown
### ~~2. B1 — 큐 진행 상황 표시~~ · **완료 (2026-08-18)**

`GET /jobs/summary` 추가(스키마 변경 없음, `idx_jobs_status_created` 재사용).
화면은 "대기 N건 · 실행 중 N건"과 남은 시간 추정을 보여준다.
추정은 최근 완료 20건의 평균에서 내고, **표본 3건 미만이면 추정하지 않는다.**
`cancelled` 는 평균에서 제외한다 — 대기 중 취소된 작업은 `started_at` 이 없어
평균을 0쪽으로 무너뜨린다.
진행률은 전체 이력이 아니라 최근 24시간 완료분 + 남은 작업에 대해 낸다
(작업 이력이 영구 보존이라 전체 대비 진행률은 무의미하다).
```

B2와 A3도 같은 방식으로 갱신한다. 남는 항목은 A1·A2 둘뿐이 되어야 한다.

- [ ] **Step 5: 커밋한다**

```bash
cd /mnt/raid/ruci_workspace/frwaler-delivery
git add docs/DELIVERY_READINESS_20260818.md
git commit -m "docs(delivery): close B1, B2 and A3

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

- [ ] **Step 6: 최종 상태를 확인한다**

```bash
cd /mnt/raid/ruci_workspace/frwaler-delivery
git status --short          # 비어 있어야 한다
md5sum delivery/fe/tsconfig.json delivery/fe/next-env.d.ts
git log --oneline -10
docker compose -p libertree-delivery ps --format '{{.Name}}\t{{.Status}}'
docker exec libertree-delivery-postgres-1 psql -U libertree -d libertree -tAc 'select count(*) from documents'
```

Expected: 작업 트리 깨끗함, `tsconfig.json`/`next-env.d.ts` 해시가 시작 시점과 동일, 운영 스택 컨테이너 3개 그대로, 문서 **536056**건.

---

## Self-Review

**1. 스펙 커버리지**

| 스펙 항목 | 태스크 |
|---|---|
| B1 — BE 집계 쿼리 | Task 1, 2 |
| B1 — FE 표시 | Task 3, 4 |
| B2 — `DELETE /schedules/{site_id}` | Task 5 |
| B2 — FE 버튼 | Task 6 |
| A3 — 전제 확정 | Task 7 |
| 런북 5일 낡음 | Task 7 Step 4 |
| A1 · A2 | **범위 밖** — 위 "이 계획에 없는 것"에 사유 기재 |

`GET /jobs` 의 `limit` 상한 200은 그대로 둔다. 스펙이 요구한 것은 "총 대기 건수와 남은 시간"이었고, 그것은 집계로 해결된다. 804행을 한 번에 내려받는 것은 화면이 쓸 수 없다.

**2. 플레이스홀더 점검** — TBD·TODO·"적절한 오류 처리" 없음. 모든 코드 단계에 실제 코드 블록이 있음. Task 6 Step 5만 "파일을 읽고 구조에 맞추라"고 지시하는데, 이는 대상 파일이 압축된 JSX를 쓰기 때문이며 붙일 코드 자체는 전부 제시했다.

**3. 타입 일관성**
- `summarize_queue` 반환 키 5개(`counts`/`active`/`finished_24h`/`avg_seconds`/`samples`) → Task 2가 그대로 반환 → Task 3의 `parseQueueSummary` 가 같은 이름으로 읽음 → Task 4 테스트의 `summary` 상수도 같은 5개 키. 일치.
- `schedules.delete(conn, site_id) -> bool` → Task 5의 `delete_schedule` 이 `if not schedules.delete(...)` 로 소비. 일치.
- `QueueProgress` 필드(`queued`/`running`/`active`/`finished`/`percent`/`etaText`/`headline`) → Task 4의 JSX가 `progress.headline`, `progress.etaText`, `progress.percent`, `progress.active` 만 사용. 전부 정의됨.
- `formatEta(seconds, activeJobs)` 인자 순서가 Task 3 테스트와 구현에서 동일.
- `operatorHeaders()` — 인자 없이 호출하면 `content-type` 없이 토큰만. DELETE는 본문이 없으므로 맞다.
