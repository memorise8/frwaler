# 진행사항 종합 보고 (2026-08-05)

> 이 세션(케파 산정 → 크롤러 건강검진 → 수리)의 완결 보고서.
> 상시 참조: `docs/CAPACITY_PIPELINE.md`(케파 상세) · `docs/HANDOFF.md`(프로젝트 전체) · `scripts/audit/CRAWLER_REPAIR_20260805.md`(수리 상세).
> 전 과정 **count-only(무저장)·DB 읽기전용** — DB documents 525,570 불변, blob 증가 0.

---

## 0. 한눈에

| 트랙 | 결과 |
|---|---|
| **케파(전량 규모) 산정** | 완료 → **가용 하한 22,923,093건 / ≈84TB** (보유 525,570 = 커버리지 2.3%) |
| **크롤러 건강검진 (804)** | 완료 → **정상 718/778 (92%)** |
| **broken 크롤러 수리** | 44개 중 **27 수리 / 17 인프라차단** (독립 재검증 완료) |
| Git | `libertree` 브랜치, 이 세션 6커밋 푸시 (최신 `d435c46`) |

---

## 1. 케파(받을 수 있는 전체 데이터) 산정 — 완료

**배경**: 업체가 "50만이 전부가 아니다" → 804개 사이트 전량 규모를 측정해 스토리지 산정.

### 3단계 파이프라인 (전부 완료)
1. **A/B (API 정확측정)** = 114개 / **16.08M** (DOAJ server_total 13.36M 포함)
2. **C+E sweep** (HTML 페이징 544개, 2h/사이트, 재개형) = 완료
3. **exact_probe** (544개 전체 정밀) = 정확값 293개(2.15M), 실패 251개는 사유 기록

### 최종 수치 (`scripts/audit/capacity_final_report.xlsx`, 5시트)
- **가용 총계(하한): 22,923,093건** — exact 591개 22.73M / 과소치 211개 0.19M
- **전량수집 필요용량: ≈84 TB** (현재 여유 3.6TB → gdrive 1PB 마운트 후보)
- DOAJ 제외 시 9.56M

### 3대 대형 (백만 급)
| 규모 | 사이트 | 근거 |
|---:|---|---|
| 13,362,110 | DOAJ | server_total |
| 1,709,118 | e-stat (일본 통계) | API 측정 |
| **1,561,276** | **ots.at (오스트리아 통신사)** | exact_probe 신규발견 → API totalCount 직접검증 |

### 핵심 도구 (재현·재개 가능)
- `scripts/audit/count_only_harness.py` — count-only 하네스(캡무력화 + server_total 포착 + `--probe N` 건강검진 모드)
- `scripts/audit/run_c_full.py` — C sweep 런처(재개형, MASTER/PART fold)
- `scripts/audit/exact_probe.py` — 정밀 측정(total_text / lastpage_verified / batched list_walk). **HTTP후킹으로 리스트URL 자동추출, 상세페이지 안 받음**
- `scripts/audit/run_exact_chunked.py` — 40개 청크 격리 실행(단일프로세스 544연속 FD고갈 대응)
- `scripts/audit/finalize_capacity.py` — 병합 규칙 **max(sweep, probe, server_total)** → 보고서
- 실패 사유: `tests.xlsx` 'exact_probe_실패' 시트 (251개 사이트별)

---

## 2. 크롤러 전체 건강검진 (804) — 완료

**방식**: sweep 실증(counted≥3) 재사용 + 나머지 308개 실전 probe(`--probe 3`, 300s, 무저장). 0건은 로그로 세분류.

### 검진 결과 (`scripts/audit/crawler_health_full.csv`, `CRAWLER_HEALTH.md`)
| health | 수 | 의미 |
|---|---:|---|
| **ok** | **718** | 정상 작동 (수리 반영 후) |
| blocked / blocked_or_dead | 25 | 사이트가 차단/폐쇄 (크롤러 문제 아님) |
| no_crawler | 26 | D그룹(datos.gob.mx 계열 등) 미제작 |
| pdf_artifact | 16 | PDF의존 크롤러 — count-only 측정한계로 판정보류(실수집시 확인) |
| slow_unknown | 15 | 5분내 무저장(느림/고장 미확정) |

---

## 3. broken 44개 수리 — 27 수리 / 17 차단

6개 병렬 에이전트 수리 → **독립 재검증**(`.env` 소싱 count-only probe, counted≥1 기준).

### 근본 원인 (코드 노후가 아니라 외부 변화가 대부분)
- **플랫폼 이전 ~11**: SPA/JSON API/OAI 전환 → 새 엔드포인트 재작성 (gov.gr, rijksoverheid→open.overheid, 캐나다 공공안전부→Canada.ca, IMT Lucca→IRIS OAI, unidata SharePoint 탈피, ASTRON→OpenAlex 등)
- **WAF/봇방어 ~8**: curl_cffi TLS위장 / Playwright 상주로 돌파 (arcep, inria, nofima, eprints-soton, 독일재무부 등)
- **`.env` 오탐 4**: 코드 정상, probe가 API키 미로딩 (education-nz, phfscience-nz, inrae, gov-ie)

### 미해결 17 = 전부 인프라 차단 (`CRAWLER_REPAIR_20260805.md`)
- **IP/ASN 차단·IP밴 9**: 다른 egress/프록시 필요 (한국 지자체 3, NIH 3, regjeringen, opendatacommunities, cerema)
- **WAF JS챌린지 5**: 헤드리스로도 불가 (중국 iwhr 2, 그리스 SiteGround 3)
- **CAPTCHA 격상 2**: 우리 반복테스트로 유발 — 실제 느린페이스면 통과 가능성, 단독 재검증 대상 (벨기에 data-gov-be·om-mp)
- **임시폐쇄 1**: 아일랜드 GSI(8월말 복귀)

주의: 일부 수리 크롤러(nofima, eprints-soton, arcep, inria 등)는 **헤드리스 브라우저 상주** 방식이라 전량 수집시 다른 크롤러보다 무거움. ASTRON은 원본 소멸로 **OpenAlex 대체**(출처 변경).

---

## 4. 다음 작업 (우선순위)

1. **D 26곳 크롤러 제작** (datos.gob.mx CKAN 계열 — API가 total 즉시 반환, 쉬움) → 케파에 소폭 추가
2. **전량 다운로드** (최대 작업): 84TB 스토리지 확보 → env 캡 상향(`LIBERTREE_MAX_PAGES`/`LIBERTREE_MAX_WALL_S`) → 수집 실행
   - 실행 체크리스트 = `crawler_health_full.csv` (ok 718 우선, pdf_artifact 16·slow 15 모니터링)
3. **차단 사이트 트랙** (17 + blocked 8): 프록시/다른 IP egress로 재시도, CAPTCHA 격상 2곳은 느린페이스 단독 재검증
4. **미해결 잔여 재검진**: pdf_artifact 16(실수집으로만 확진), slow_unknown 15
5. 납품 패키지 / OCR / 봇차단 쿠키 트랙 (HANDOFF §12)

---

## 5. 이 세션 커밋 (libertree 브랜치)
- `d435c46` fix: broken 크롤러 27/44 수리
- `f1e4510` feat: 크롤러 함대 건강검진
- `ea5ab34` feat: 케파 최종 보고서 22.92M
- `cd78231` docs: HANDOFF 8/4 갱신
- `3370a22` feat: 케파 파이프라인(sweep+exact_probe)+문서
- (8/4) `847fd2a` fix: 크롤러 13 수리 / `47c2496` feat: 웹앱 files/docs/report/번역토글
