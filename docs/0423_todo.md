# Fino 프로젝트 TODO (2026-04-23)

> 이전 세션: `docs/0422_todo.md` 참조
> 브랜치: `fino-crawler` (원격 push 완료: 커밋 `35843a5`)

---

## 이전 세션 (0422) 완료 항목

- [x] cron 전체 제거 (개발 단계라 자동화 보류)
- [x] `doc_index` 테이블 스키마 + 헬퍼 함수 추가 (`crawler/db.py`)
- [x] `scan_index()` 메서드 추가 (`crawler/sites/nts_taxlaw.py`)
- [x] `scan-index` CLI 명령 (`crawler/main.py`)
- [x] `scripts/analyze_gap.py` — doc_index vs papers vs exports 3-way 비교
- [x] 전체 scan-index 실행 (pd + qt 병렬)
- [x] B 카테고리 샘플 검증 (`scripts/investigate_b.py`)
- [x] 전체 품질 체크 (필드별 누락 현황)
- [x] detail API 샘플 덤프 + 파싱 진단 (`scripts/diagnose_api.py`)
- [x] 대량 commit + push (`docker/`, `finolaw/`, `api/`, 기타 누적분)

---

## 현재 상태 스냅샷

### DB 수집 현황

| site | papers | doc_index (active) | MD 파일 | HTML symlink |
|------|-------:|-------------------:|--------:|-------------:|
| nts-taxlaw-pd | 149,348 | 135,493 | 141,297 | 141,295 |
| nts-taxlaw-qt | 138,537 | 131,113 | 128,498 | 127,522 |

### 진단으로 확인된 사실 (매우 중요)

**A. 100% 빈 값 필드 재판정:**

| 필드 | 재판정 |
|------|-------|
| `referencedCases` (참조판례) | ✅ **API 원래 안 줌** — 정상 |
| `citedCases` (인용판례) | ✅ **API 원래 안 줌** — 정상 |
| `attachedFiles` (첨부파일) | ✅ **API 원래 안 줌** — 정상 |
| `keywords` 93-98% 빈 것 | ✅ **API 원래 안 줌** — 정상 |
| `relatedLaws` 11% 빈 것 | ✅ **API 일부 미제공** — 정상 |
| **`relatedTopics` 100% 빈 것** | 🔴 **파서 버그 — 키명 불일치** |

**B. detail API의 실제 키 구조 (샘플 덤프로 확인):**

```
dcmRltnStttMatrList:
  ntstTextMatrCntn = '납부세액', '결정 및 경정', '가산세의부과'  ← 실제 값
  ntstTextNm        = None                                        ← 우리 파서가 찾는 키 (틀림)
  matrCntn          = (키 자체 없음)                              ← 우리 파서가 찾는 또 다른 키
```

**C. gap_fill의 숨은 버그:**

- detail API의 `dvo`에는 `ntstTlawClNm` (세법분류), `dcmRgtDtm` (등록일시) 필드가 **None**
- 이 필드들은 **list API에만 존재** (`NTST_TLAW_CL_NM`, `DCM_RGT_DTM`)
- gap_fill이 detail만 믿고 list 데이터를 버림 → category/published_date 누락

### 3-way gap 집계 (2026-04-22 스캔 기준)

| 카테고리 | pd | qt | 합계 |
|---------|----:|----:|----:|
| A. 미수집 (list에 있고 DB에 없음) | 490 | 834 | **1,324** |
| B. 스캔 누락 (DB에 있고 doc_index에 없음) | 14,345 | 8,258 | **22,603** |
| C. rawHtmlPath 누락 | 2 | 977 | **979** |
| D. MD 파일 누락 | 8,051 | 10,039 | **18,090** |
| E. HTML symlink 누락 | 8,053 | 11,015 | **19,068** |

리포트 원본: `reports/gap_nts-taxlaw-{pd,qt}.*.json`

### DB 품질 지표

| 필드 | pd 누락 | qt 누락 | 비고 |
|------|-------:|-------:|------|
| relatedTopics | 149,348 (100%) | 138,537 (100%) | 파서 버그 |
| published_date | 4,277 (2.9%) | 9,838 (7.1%) | gap_fill 버그 |
| category (세법분류) | 4,277 (2.9%) | 9,843 (7.1%) | gap_fill 버그 |
| rawHtmlPath | 2 (0.0%) | 977 (0.7%) | gap_fill 버그 |
| documentNumber | 0 | 975 (0.7%) | gap_fill 버그 |
| abstract 빈 값 | 0 | 223 (0.2%) | 부분 누락 |
| abstract <500자 | 5,439 (3.6%) | 24,429 (17.6%) | qt 얕은 본문 많음 |
| HTML 파일 <500바이트 | 0 | 10,835 | qt HTML 사실상 빈 파일 |

### B 카테고리 샘플 검증 결과 (각 15건)

| site | ok (detail 접근 가능) | empty (진짜 삭제) |
|------|---------------------:|-----------------:|
| pd | 15 / 15 (100%) | 0 |
| qt | 11 / 15 (73%) | 4 (old legacy DOC_ID) |

→ B 카테고리의 대부분은 **스캔 자체가 놓친 것** (페이지네이션 드리프트 추정). detail API로는 여전히 접근 가능.

---

## TODO — 0423 세션

### 파서/로직 수정 (선행)

#### ☐ 1. `relatedTopics` 키명 수정

파일: `crawler/sites/nts_taxlaw.py`

**crawl() 함수 (420~423줄 근처)**
```python
# 현재 (틀림)
for matr in (detail.get("dcmRltnStttMatrList") or []):
    name = matr.get("ntstTextNm") or matr.get("matrCntn") or ""

# 수정
for matr in (detail.get("dcmRltnStttMatrList") or []):
    name = matr.get("ntstTextMatrCntn") or ""
```

**gap_fill() 함수 (665~666줄)** — 같은 수정

#### ☐ 2. gap_fill 구조 수정 (category / published_date / rawHtmlPath)

현재 gap_fill은 detail API만 사용. list API에도 있는 필드들을 놓치고 있음.

**방안 A**: gap_fill Phase 1(list 스캔)에서 각 doc_id별 list-item 전체를 캐시 → Phase 3(detail fetch) 때 list 데이터 + detail 데이터 병합

**방안 B**: gap_fill을 완전히 `crawl()`과 동일한 구조로 바꾸고, 중복 DOC_ID만 스킵하는 형태로 단순화

→ **방안 A 권장** (코드 최소 변경)

#### ☐ 3. 수정한 파서로 샘플 1~10건 재fetch 테스트

- 1건으로 실제 DB 업데이트 확인
- relatedTopics, category, published_date 모두 채워지는지 검증
- 문제없으면 다음 단계

### 타겟 재fetch (소규모)

#### ☐ 4. A 카테고리 + gap_fill 버그 레코드 재fetch (~15,000건)

대상:
- A. 미수집 1,324건 (신규 fetch)
- published_date 누락: pd 4,277 + qt 9,838 = 14,115건
- rawHtmlPath 누락: qt 977건 (위와 일부 중복)
- documentNumber 누락: qt 975건 (rawHtmlPath와 거의 일치)

**중복 제거 후 예상 ~15,500건**

예상 시간: 3~6시간 (delay 1초 기준)

**구현**: `scripts/targeted_refetch.py` 신규 — DOC_ID 리스트 입력받아 detail fetch + DB 업데이트

### 대규모 백필 (선택, 시간 많이 듦)

#### ☐ 5. `relatedTopics` 백필 (288K 전체)

옵션:
- (A) 전체 재fetch: 80시간 (delay 1.0) / 40시간 (delay 0.5) / 24시간 (delay 0.3)
- (B) pd+qt 병렬: 위 시간 절반. 단, 같은 서버라 rate-limit 위험 있음
- (C) 야간/주말 등 여유 시간에 분할 실행

**전제 필요**:
- 체크포인트 저장 (마지막 처리 external_id)
- empty response 반복 시 자동 delay 증가
- 기존 필드 보존 (metadata.relatedTopics만 업데이트)

**구현**: `scripts/enrich_related_topics.py` — 전용 백필 스크립트

사용자 의사결정 필요. **나중에 결정 후 진행.**

### 후속 정리

#### ☐ 6. MD/HTML export 재실행

재fetch로 추가/수정된 레코드 반영.
```bash
.venv/bin/python scripts/export_papers_md.py --site-id nts-taxlaw-pd
.venv/bin/python scripts/export_papers_md.py --site-id nts-taxlaw-qt
```
또는 증분 export 옵션 추가 (TODO 0422 #1번 연계).

#### ☐ 7. doc_index 재스캔 (B 카테고리 해소)

파서 수정 후 1회 더 scan-index 실행하면 페이지네이션 누락이 줄어들 수 있음.
또는 `doc_index`를 detail API에서 확인된 DOC_ID로 보강하는 별도 스크립트.

#### ☐ 8. qt HTML <500바이트 파일 10,835건 조사

- 실제 빈 페이지인지, 특정 카테고리(예: 고시/기준) 특성인지 샘플 확인
- 필요시 해당 레코드만 재fetch 목록에 추가

---

## 실행 중인 작업 없음

이전 세션에서 돌린 scan-index 2건은 모두 **완료**:
- `b831ydogn` (pd): 완료 (exit 0)
- `bcqvfy6a0` (qt): 완료 (exit 0)

cron 없음. 수동 실행만 해당.

---

## 주요 파일 참조

| 파일 | 역할 |
|------|------|
| `crawler/sites/nts_taxlaw.py` | 크롤러 본체 (crawl + incremental + gap_fill + **scan_index**) |
| `crawler/main.py` | CLI (crawl, stats, download, convert, **scan-index**) |
| `crawler/db.py` | DB 스키마 + upsert_doc_index/mark_doc_index_deleted/get_doc_index_stats |
| `scripts/export_papers_md.py` | DB → MD/HTML export |
| `scripts/report_incomplete.py` | 불완전 수집 리포트 |
| `scripts/analyze_gap.py` | **3-way gap 분석 (신규)** |
| `scripts/investigate_b.py` | **B 카테고리 샘플 검증 (신규)** |
| `scripts/diagnose_api.py` | **detail API 응답 덤프 + 키 구조 진단 (신규)** |
| `reports/gap_*.json` | gap 카테고리별 DOC_ID 리스트 |
| `reports/detail_*.json` | detail API 원본 샘플 (4건) |

## 빠른 시작 (새 세션)

```bash
cd /data_raid/ruci_workspace/crawler-poc

# 1. 현재 상태
.venv/bin/python -m crawler.main stats
.venv/bin/python -c "
import sqlite3
con = sqlite3.connect('data/papers.db')
for r in con.execute('SELECT site_id, COUNT(*) AS total, SUM(CASE WHEN deleted_at IS NULL THEN 1 ELSE 0 END) AS active FROM doc_index GROUP BY site_id'):
    print(tuple(r))
"

# 2. 이 문서 읽기
cat docs/0423_todo.md

# 3. gap 리포트 요약
ls reports/gap_*.summary.json

# 4. 진단용 detail API 샘플
ls reports/detail_*.json
```

## Git 커밋 히스토리 (fino-crawler 브랜치)

```
35843a5 Add doc_index scanner, finolaw app, docker setup, and agent/docs cleanup
65dcbda Add doc_index and gap analysis to TODO
767164f Add 0422 TODO: 98.5% achieved, next steps documented
640f023 Fix gap_fill documentTypeName mapping and backfill 14,115 records
44f6d56 Add --gap-fill mode for fast missing document recovery
1ce8ab2 Add gap-fill mode and xlsx comparison tool to TODO
9e33a6c Add TODO checklist for post-gap-fill work (0421)
4d9d288 Fix MD export filename collision — append external_id for uniqueness
a5a1792 Fix TLS and attribute name for incremental crawling
5d1ba49 Add NTS taxlaw crawler, incremental mode, MD export, and documentation
```
